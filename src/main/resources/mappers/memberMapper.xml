<?xml version="1.0" encoding="UTF-8"?>
<!-- DTD지정  -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
<mapper namespace="com.bestpricemarket.mappers.memberMapper">
	<insert id="joinMember">
		insert into bestpricemarket(id,pw,username,email,phone,addr,reg_date) 
      values (#{id},#{pw},#{username},#{email},#{phone},#{addr},now())
	</insert>
	
	<select id="readMemberWithIDPW" resultType="com.bestpricemarket.domain.MemberVO">
		select *
		from bestpricemarket
		where id=#{id} and pw=#{pw}
	</select>	
	
</mapper>